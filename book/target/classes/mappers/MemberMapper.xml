<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.book.Repository.MemberRepository">

	<insert id="save" parameterType="com.book.DTO.MemberDTO">
		insert into member2(user_id, password, email ) 
		  values( #{userId}, #{password},  #{email} )
	</insert>

</mapper>